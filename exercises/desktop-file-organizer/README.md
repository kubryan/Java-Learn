# 桌面檔案整理工具：練習起點

先執行 `FirstWindow.java`，確認視窗能顯示且按鈕能改變狀態文字。這代表你已經完成「元件 → 事件 → 方法」的第一個循環。

下一步請按以下順序加入功能：

1. 使用 `JFileChooser` 選擇資料夾。
2. 讀取資料夾清單，只顯示檔案名稱，不進行移動。
3. 建立「預覽整理結果」功能，先讓使用者確認。
4. 最後才加入真正移動檔案的操作與復原紀錄。

> 涉及檔案移動時，不要先寫刪除或覆寫行為。先做預覽、確認與錯誤提示。
